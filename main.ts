basic.forever(function () {
    for (let Y = 0; Y <= 4; Y++) {
        led.plot(0, Y)
        basic.pause(200)
        basic.clearScreen()
    }
    for (let Y = 0; Y <= 4; Y++) {
        led.plot(1, Y)
        basic.pause(200)
        basic.clearScreen()
    }
    for (let Y = 0; Y <= 4; Y++) {
        led.plot(2, Y)
        basic.pause(200)
        basic.clearScreen()
    }
    for (let Y = 0; Y <= 4; Y++) {
        led.plot(3, Y)
        basic.pause(200)
        basic.clearScreen()
    }
    for (let Y = 0; Y <= 4; Y++) {
        led.plot(4, Y)
        basic.pause(200)
        basic.clearScreen()
    }
    basic.showIcon(IconNames.Yes)
    for (let Y = 0; Y <= 4; Y++) {
        led.plot(0, Y)
        basic.pause(120)
        basic.clearScreen()
    }
    for (let Y = 0; Y <= 4; Y++) {
        led.plot(1, Y)
        basic.pause(120)
        basic.clearScreen()
    }
    for (let Y = 0; Y <= 4; Y++) {
        led.plot(2, Y)
        basic.pause(120)
        basic.clearScreen()
    }
    for (let Y = 0; Y <= 4; Y++) {
        led.plot(3, Y)
        basic.pause(120)
        basic.clearScreen()
    }
    for (let Y = 0; Y <= 4; Y++) {
        led.plot(4, Y)
        basic.pause(120)
        basic.clearScreen()
    }
    basic.showIcon(IconNames.Yes)
    for (let Y = 0; Y <= 4; Y++) {
        led.plot(0, Y)
        basic.pause(40)
        basic.clearScreen()
    }
    for (let Y = 0; Y <= 4; Y++) {
        led.plot(1, Y)
        basic.pause(40)
        basic.clearScreen()
    }
    for (let Y = 0; Y <= 4; Y++) {
        led.plot(2, Y)
        basic.pause(40)
        basic.clearScreen()
    }
    for (let Y = 0; Y <= 4; Y++) {
        led.plot(3, Y)
        basic.pause(40)
        basic.clearScreen()
    }
    for (let Y = 0; Y <= 4; Y++) {
        led.plot(4, Y)
        basic.pause(40)
        basic.clearScreen()
    }
    basic.showIcon(IconNames.Yes)
})
